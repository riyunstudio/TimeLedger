# =============================================================================
# Docker 容器環境變數
# 使用方式: docker run --env-file .env.docker ...
# =============================================================================

# === Backend ===
APP_ENV=production
APP_DEBUG=false
APP_TIMEZONE=Asia/Taipei
APP_ID=1
SERVER_NAME=timeLedger
SERVER_HOST=0.0.0.0
SERVER_PORT=8888

# JWT Secret (請更換為安全的隨機字串)
JWT_SECRET=your-production-jwt-secret-here

# MySQL (使用 docker network 內的 service name)
MYSQL_MASTER_HOST=mysql
MYSQL_MASTER_PORT=3306
MYSQL_MASTER_USER=root
MYSQL_MASTER_PASS=timeledger_root_2026
MYSQL_MASTER_NAME=timeledger

MYSQL_SLAVE_HOST=mysql
MYSQL_SLAVE_PORT=3306
MYSQL_SLAVE_USER=root
MYSQL_SLAVE_PASS=timeledger_root_2026
MYSQL_SLAVE_NAME=timeledger

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASS=timeledger_redis_2026
REDIS_POOlSIZE=100

# === Frontend ===
NODE_ENV=production
NUXT_HOST=0.0.0.0
NUXT_PORT=3000
API_BASE_URL=http://localhost:8888/api/v1

# LINE LIFF
NUXT_PUBLIC_LIFF_ID=2009043973-1udoR0bZ
NUXT_PUBLIC_LINE_OFFICIAL_ACCOUNT_ID=@373nmzzt
